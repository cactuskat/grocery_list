## Entry [1] — [Adding Starting Files] ([6/8/26], tool: Claude) **Context:** Starting project with basic files.  **Prompt Excerpt:** "So let's add a way for the user to cross things off the grocery list. put a small checkbow next to items instead of a trash icon to remove from grocery list. but i would like it so the items only disappear after you leave the grocery list, in case you hit it accidentally" **AI Summary:** Provided a code that included the checkbox next to it **Human Evaluation:** had a major bug that that had the code modifying a list while iterating over it. rejected the AI-generated implementation as-is. **Final Decision:** Kept the checkbox interaction idea, but manually corrected the list update logic to avoid modifying the list while iterating over it. **Testing Verification:** Tested adding items to the grocery list, checking items off, leaving the grocery list, and returning to confirm the correct items were removed without crashes or skipped items.**Commit:** 7ac04c8

## Entry [2] — [Replaced How Database Worked] ([6/9/26], tool: ChatGPT) **Context:** I had built a good foundation for the app, but hit a roadblock when trying to add sqlite after. I decided to remake the entire project, with sqlite at its base to start. **Prompt Excerpt:** "so i want to create a flutter project that acts a a grocery list. the items will be from an api and the data will be saved using sqlite. the end goal is the user is going to get a list of items, select which to add to th list, the list is saved as persistent data, then the use will go to the second page and see the list. if an item is checked, then it is removed from the grocery list and is returned as visible back to the selection menu. to start, what are 3 good way to save the data in sqlite? how it is done?" **AI Summary:** Code for a 3 SQL table options that holds the selected items chosen. **Human Evaluation:** I weighted the costs and scope of the complete project. The goal was to have items move between lists, not have a history or undo. **Final Decision:** I chose deleting checked items over storing a isChecked column because checking an item means it should return to the list. The cost is no completed-item history. This holds up until the app needs to undo, history, or any sort of tracking. **Testing Verification:** Tested by selecting every item and seeing it it holds in the table outside of connection of API and if it persists when the program closes. **Commit:** 663413c

## Entry [4] — [Crashing upon Loading] ([6/9/26], tool: Chatgpt) **Context:** Adding SQLite storage to my Flutter grocery list app so that grocery items could be saved and viewed later. When I tried to load saved items, the app crashed with a type error. Challenge was figuring out whether the problem was in the code or in the existing database. **Prompt Excerpt:** This is a flutter app using SQLite. Fix a crash when loading saved grocery items. The app already had a database created from earlier testing and is running on VSCOde Web. Success: Grocery items load correctly without crashing. Do not rebuild the entire app. **AI Summary:** AI suggested that the value being read from SQLite was stored as a different type than the code expected. It also suggested that an older version of the database could be causing the problem. **Human Evaluation:** Checked the database setup and realized code was correct. REalized prior databse file was conflicting with changed table structure. **Final Decision:** Added a database upgrade method so that when the database version changes, the old table is removed and recreated using the new structure. This fixed the crash and made future database changes easier to manage **Testing Verification:** increased the database version number, ran the app, added grocery items, and confirmed they could be saved and loaded without errors **Commit:** aad8057

## Entry [4] — [UI Changes to a consistent theme] ([6/9/26], tool: Gemini) **Context:** The current app's UI felt too inorganic with no flow.It required too much thought. With only changing the aesthethic, the risk of code breaking increases. **Prompt Excerpt:** Make a more user-friendly inteface. Choose a green theme that sticks with grocery stores.Do not change the code nor the funcitons. Make it look more professional. **AI Summary:** A refreshed app with a modern aestheitc, but some parts of the code no longer functioned. **Human Evaluation:** The AI made a more professional looking app, but the UI still felt odd. Some features were no longer working.  **Final Decision:** I redid the prompt after designing what I wanted exactly on paper. **Testing Verification:** Retried every feature that was added to ensure nothing changed. **Commit:** 932e3ea